services:
  docs:
    image: cookie_project_local_docs
    container_name: cookie_project_local_docs
    build:
      context: .
      dockerfile: ./compose/local/docs/Dockerfile
    env_file:
      - ./.envs/.local/.django
    volumes:
      - /app/.venv
      - ./docs:/docs:z
      - ./config:/app/config:z
      - ./cookie_project:/app/cookie_project:z
    ports:
      - '9000:9000'
    command: /start-docs
